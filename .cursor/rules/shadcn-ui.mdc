# shadcn/ui Guidelines - СТРОГИЕ ПРАВИЛА

## ⚠️ ВАЖНО: АБСОЛЮТНО ЗАПРЕЩЕНО СОЗДАВАТЬ КАСТОМНЫЕ UI КОМПОНЕНТЫ

Этот проект использует **ТОЛЬКО** shadcn/ui для всех UI элементов. Создание собственных UI компонентов категорически запрещено.

## 🚫 ЧТО ЗАПРЕЩЕНО

- Создавать кастомные кнопки, инпуты, карточки или любые другие UI элементы
- Писать CSS стили для UI компонентов (кроме глобальных стилей)
- Использовать HTML элементы без обертки в shadcn/ui компоненты
- Создавать собственные модальные окна, dropdown'ы, или другие сложные UI элементы

## ✅ ЧТО ОБЯЗАТЕЛЬНО

- Использовать **ТОЛЬКО** компоненты shadcn/ui
- Для Clerk аутентификации использовать shadcn/ui Button компоненты
- Clerk должен запускаться через модальные окна (используя shadcn/ui Dialog)
- Все UI элементы должны быть из shadcn/ui библиотеки

## 🔧 Инициализация shadcn/ui

Если shadcn/ui не инициализирован в проекте:

```bash
npx shadcn@latest init
```

## 📦 Установка компонентов

Если нужный компонент не установлен:

```bash
npx shadcn@latest add [component-name]
```

Примеры:

- `npx shadcn@latest add button` - для кнопок
- `npx shadcn@latest add card` - для карточек
- `npx shadcn@latest add input` - для полей ввода
- `npx shadcn@latest add dialog` - для модальных окон
- `npx shadcn@latest add form` - для форм

## 🎯 Clerk Integration

Для Clerk аутентификации:

1. Используй shadcn/ui Button компоненты для Sign In/Sign Up
2. Используй shadcn/ui Dialog для модальных окон аутентификации
3. НЕ создавай кастомные формы аутентификации

## 📁 Важные файлы

- [package.json](mdc:package.json) - зависимости проекта
- [src/app/layout.tsx](mdc:src/app/layout.tsx) - корневой layout
- [src/app/globals.css](mdc:src/app/globals.css) - глобальные стили
- [src/components/ui/](mdc:src/components/ui/) - папка с shadcn/ui компонентами

## ⚡ Быстрый старт

Если нужен новый UI элемент:

1. Проверь, есть ли он в shadcn/ui
2. Если нет - установи через `npx shadcn@latest add [component]`
3. НИКОГДА не создавай кастомный компонент

## 🚨 Нарушение правил

Создание кастомных UI компонентов приведет к:

- Отклонению кода
- Необходимости переписывания
- Нарушению архитектуры проекта

**ПОМНИ: ТОЛЬКО shadcn/ui - НИКАКИХ КАСТОМНЫХ UI!**
